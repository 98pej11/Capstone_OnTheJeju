spring:
  profiles:
    active: test
#  jpa:
#    hibernate:
#      ddl-auto: update
  redis:
    lettuce:
      pool:
        min-idle: 0
        max-idle: 8
        max-active: 8
    port: 16379
    host: localhost
#이거 테스트 코드에서 설정해 줘야함 ㅋㅋ
jwt:
  secret: webfirewood


